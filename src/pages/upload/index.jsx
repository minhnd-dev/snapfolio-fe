export function Upload() {
    return (
        <>
        <p> Nothing to see here</p>
            {/*
            <input type="file" id="avatar" name="file" accept="image/png, image/jpeg"  onChange={(e) => {uploadedFile = e.target.files[0]}}/>
            <button onClick={upload}>Upload</button>
            */}
        </>
    )
}
